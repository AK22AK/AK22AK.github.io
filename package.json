{
  "name": "ak22ak-portfolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync:data": "bash scripts/sync-data.sh ../daily-news-data --optional",
    "predev": "npm run sync:data --silent",
    "dev": "astro dev",
    "prebuild": "npm run sync:data --silent",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.8"
  }
}
