# daily-news 数据目录

此目录下的 JSON 文件由数据仓库 [`AK22AK/daily-news-data`](https://github.com/AK22AK/daily-news-data) 自动生成并同步。

**请勿手动修改此目录下的文件**，它们会在每次构建时被覆盖。

本地开发时，运行根目录的 `scripts/sync-data.sh` 从数据仓库同步最新数据。
